version: '3'
services:
  website:
    image: ${CONTAINER_REGISTRY}perfectengineering/asowunmi-website:${WEBSITE_VERSION}
    build:
       context: .
       dockerfile: Dockerfile
    ports:
      - "80:80"
    restart: always
